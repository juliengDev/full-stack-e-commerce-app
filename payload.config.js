import { buildConfig } from 'payload/config'

export default buildConfig({
  collections: [
    {
      slug: 'mycollection',
      // autres configurations de la collection ici
    },
    // d'autres collections
  ],
  // autres options de configuration
})
